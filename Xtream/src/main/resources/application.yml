spring:

  application:
    name: Xtream

  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:

  batch:
    initialize-schema: always

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      format_sql: true

Xtream:

  EventOne:
    size: 20

  EventTwo:
    size: 20
